<!-- 内容 -->
<view class="container">
  <!-- Type-specific content -->
  <view class="type-info" wx:if="{{type === '1'}}">
    <text class="dynasty-title">朝代：唐朝</text>
    <view class="dynasty-details">
      <text>朝代年份：618-907</text>
      <text>首都：长安</text>
      <text>现在的位置：陕西省</text>
    </view>
  </view>
  
  <view class="type-info" wx:if="{{type === '2' || type === '3'}}">
    <text>收录数量：{{poems.length}}</text>
  </view>

  <view class="type-info" wx:if="{{type === '4'}}">
    <text>词牌描述：这是一种古典的诗词格式，包含独特的词牌名称和格式。</text>
  </view>
  
  <view class="type-info" wx:if="{{type === '5'}}">
    <text>作者：李白</text>
    <view class="author-details">
      <text>朝代：唐朝</text>
      <text>作品数量：200+</text>
      <text>生平介绍：李白，字太白，号青莲居士，是中国历史上最著名的诗人之一。</text>
    </view>
  </view>

  <view class="description">
    <text>这里是一个简单的介绍。</text>
  </view>
  
  <view class="poem-list">
    <view wx:for="{{poems}}" wx:key="index" class="poem-item">
      <view class="poem-details">
        <text class="title">{{item.name}}</text>
        <view class="info">
          <text class="dynasty">[{{item.dynasty}}]</text>
          <text class="author">{{item.author}}</text>
        </view>
      </view>
    </view>
  </view>
  
  <view wx:if="{{loading}}" class="loading">
    <text>加载中...</text>
  </view>
</view>